# Command Buffers

Commands in Vulkan, like drawing operations and memory transfers, are not excluded directly using function calls.
You have to record all the operations you want to perform in command buffer objects.

